# Alpha Scout - 智能股票分析系统

Alpha Scout 是一个功能强大的智能股票分析系统，提供全面的股票数据获取、分析和可视化功能，帮助投资者做出更明智的投资决策。

## 📈 未来规划

- [x] 补充情感分析模块
- [x] 补充RAG模块
- [x] 优化股票图表工具，支持更多技术指标
- [x] 优化股票信息源，目前支持yfinance，tushare, akshare
- [ ] 开发Web用户界面 (Streamlit/Web)
- [x] 实现投资组合管理 (优化配置与风险评估)
- [x] 添加回测模块 (策略验证)
- [ ] 升级RAG功能 (支持网页搜索与多种文档格式)

## 🌟 核心功能

### 1. 多源数据集成
- **中国市场**：集成 Tushare 和 Akshare 作为数据源，自动切换以确保数据可用性
- **美国市场**：使用 yfinance 获取美股数据
- **实时数据**：获取股票实时价格、基本面数据和历史行情

### 2. 基本面分析
- 公司概况：名称、行业、市盈率、市值等
- 财务报表分析：利润表、资产负债表和现金流量表
- 估值指标：PE、PB、PS、PEG等
- **RAG增强检索**：基于 ChromaDB，支持从本地财报PDF中检索关键信息 (Multi-lingual Embedding)

### 3. 技术分析
- 历史数据可视化：支持1月/3月/6月/1年等多种周期
- **技术指标增强**：自动计算并展示 MACD、RSI、MA20/MA60 等关键指标
- **本地缓存优化**：自动缓存历史数据，提升图表生成速度

### 4. 投资组合管理
- 投资组合构建
- 风险和收益评估
- 资产配置建议

### 5. 市场情绪分析
- **智能新闻分析**：集成 DeepSeek API 分析实时财经新闻
- **多源新闻获取**：支持 AkShare (东方财富/财联社) 和 yfinance 新闻源
- **量化情绪评分**：生成情感得分与关键驱动因素摘要

## 🛠️ 技术栈

- **编程语言**：Python
- **数据处理**：Pandas, NumPy
- **数据可视化**：Matplotlib, Plotly
- **Web框架**：Flask/FastAPI (根据实现)
- **数据源**：Tushare, Akshare, yfinance
- **环境管理**：Conda, pip

## 📦 安装与设置

### 1. 克隆仓库

```bash
git clone https://github.com/yourusername/alpha_scout.git
cd alpha_scout
```

### 2. 安装依赖

使用 Conda 创建环境 (推荐)：

```bash
conda create -n alpha_scout python=3.10
conda activate alpha_scout
pip install -r requirements.txt
```

或使用 pip 直接安装：

```bash
pip install -r requirements.txt
```

### 3. 配置环境变量

创建 `.env` 文件并配置必要的API密钥：

```env
# Tushare API Key
TS_TOKEN=your_tushare_token_here

# 代理设置 (可选)
HTTP_PROXY=http://127.0.0.1:7897
HTTPS_PROXY=http://127.0.0.1:7897
```

## 📁 项目结构

```
Alpha Scout/
├── core/              # 核心功能模块
│   ├── agent.py            # 智能代理逻辑 (Custom Tool-calling Loop)
│   ├── agent_state.py      # 代理状态管理
│   └── prompt_templates.py # 提示模板
├── data_source/       # 数据来源相关
│   ├── rag_data/           # RAG数据
│   └── vector_db/          # 向量数据库
├── tools/             # 工具函数
│   ├── real_time_tool.py   # 实时数据获取工具
│   ├── visualization_tool.py # 可视化工具
│   ├── rag_tool.py         # RAG工具
│   └── sentiment_tool.py   # 情感分析工具
├── utils/             # 通用工具函数
│   └── error_handlers.py   # 错误处理
├── .env               # 环境变量配置
├── requirements.txt   # 依赖列表
├── LICENSE            # 许可证
└── README.md          # 项目说明
```

## 🚀 使用方法

### 基本用法

```python
from tools.real_time_tool import get_stock_price

# 获取中国股票数据
hist, fundamentals = get_stock_price("000001.SZ")
print("基本面数据:", fundamentals)
print("历史数据行数:", len(hist))

# 获取美国股票数据
hist, fundamentals = get_stock_price("AAPL")
print("基本面数据:", fundamentals)
print("历史数据行数:", len(hist))
```

### 命令行测试

```bash
# 测试股票数据获取
python -c "from tools.real_time_tool import get_stock_price; hist, fundamentals = get_stock_price('000001.SZ'); print(fundamentals)"
```

## 📊 主要API

### `get_stock_price(ticker, market=None)`
获取股票实时价格和基本面数据

- **参数**:
  - `ticker`: 股票代码
  - `market`: 市场标识 ('cn' 或 'us')，可选

- **返回**:
  - `hist`: 历史行情数据 (Pandas DataFrame)
  - `fundamentals`: 基本面数据 (字典)

### `get_tushare_stock_data(ticker)`
从Tushare获取中国股票数据

### `get_akshare_stock_data(ticker)`
从Akshare获取中国股票数据

### `get_yfinance_stock_data(ticker)`
从yfinance获取美国股票数据

## 🔄 数据源切换机制

当Tushare达到访问限制时，系统会自动切换到Akshare获取数据：

1. 首先尝试使用Tushare获取数据
2. 如果Tushare返回错误或达到访问限制
3. 自动切换到Akshare作为备用数据源
4. 确保数据获取的连续性和稳定性

## 🎨 可视化功能

系统提供多种可视化图表：
- 股票价格走势
- 成交量分析
- 技术指标图表
- 财务报表可视化
- 投资组合表现

## 🔒 数据安全

- 所有API密钥存储在环境变量中
- 不存储敏感个人信息
- 数据获取符合各数据源的使用条款

## 🤝 贡献指南

欢迎参与Alpha Scout项目的开发！

1. Fork 仓库
2. 创建特性分支 (`git checkout -b feature/AmazingFeature`)
3. 提交更改 (`git commit -m 'Add some AmazingFeature'`)
4. 推送到分支 (`git push origin feature/AmazingFeature`)
5. 打开 Pull Request

## 📄 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情

**版权所有 (c) 2025 wenxin**

## 📞 联系我们

如有问题或建议，请通过以下方式联系：

- Email: aoiidune@qq.com
- GitHub Issues: https://github.com/leimuwo/Alpha-Scout/issues


---

**Alpha Scout** - 让投资更智能！ 🚀